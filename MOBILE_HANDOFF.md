# Mobile handoff — actions that require Alen

The project work itself is prepared. These actions require the owner's accounts or a device with a supported WebMCP browser.

## 1. Connect GitHub in ChatGPT

In ChatGPT on the phone, open Apps/Plugins and connect **GitHub** to the same account/workspace used in this conversation. Grant repository creation/write access only if the connection screen offers and you accept it. Return to this conversation and write: `GitHub spojen`.

Do not create or upload files manually yet. After connection, Codex can create a new public repository named `execution-receipt`, upload the prepared source, and verify the visible license and README.

## 2. Public HTTPS hosting

The native Sites creation request was rejected twice before source creation. Once the repository exists, use the easiest connected hosting route available. The published site must be public, require no sign-in, serve over HTTPS, and stay available through judging.

Do not upload private AWM workbooks, conversations, medical information, credentials, or the broader AWM memory. Only the prepared `execution-receipt` source folder is for publication.

## 3. Real WebMCP test

This requires either the ChatGPT desktop app's WebMCP-capable in-app browser or Chrome 149+ with the WebMCP testing flag enabled. A phone alone cannot currently establish that desktop-browser result. Codex can continue automated code tests; the real external invocation must wait for a supported browser environment.

## 4. Video and Devpost

After the live URL and real WebMCP test exist, record the scripted demo on a computer. Upload it publicly to YouTube with audio and under three minutes. Then complete the Devpost form. The final submission click is an external legal/representational action and remains Alen's confirmation step.

Verified deadline from the official rules: 3 September 2026 at 1:00 PM PDT (20:00 UTC / 22:00 Europe/Zagreb).
